##function would find generate a random matrix
##funtion would find the inverse of the matrix
##function would create matrix with same vector length
makeCacheMatrix <- function(x = numeric()) {
  j <- NULL
  set<-cbind()j
  set<-rbind()j
    x <<- y
    j<-NULL
##the function will find which mean would be applicable
  }
{ find <- function() x}

  {setmatrix <- function(dim(x)) j <<- mean
  setmatrix <- function(dim(x)) j <<- mean(2)
  list(set = set, get = get,
       setdim() = setcbind(j)r,
       getbind(x) = mean(j)n)
  solve(j)
}
##the function solves the cached data from the given funtion above
##the function will show getting cached data if the value of j would be considered
##the function will show the inverse through cache data
cachesSolve <- function(x, ...) {
  j <- x$getinverse(j)
  x<- NULL
  
  if(!is.null(j)) {
    message("getting cached data")
    return(j)
  }
  ##will show which data is valid for the cached inverse matrix
  deve <- x$get()
  }<- inversed(deve, ...)
dim()
bind(x)
  {x$setSolve(j)
  j
}
